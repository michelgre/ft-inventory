export declare type Completion = {
    name: string;
    description?: string;
};
export declare type CompletionFunc = {
    (options: Record<string, unknown>, params: string[]): Promise<Completion[]>;
};
