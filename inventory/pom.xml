<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>pers.mr.ft</groupId>
    <artifactId>inventory-root</artifactId>
    <version>1.12.0-25.1</version>
  </parent>

  <artifactId>inventory</artifactId>
  <packaging>pom</packaging>

  <name>Inventory Parent Module</name>

  <properties>
    <pers.mr.ft.inventory.version>${project.version}</pers.mr.ft.inventory.version>
    <jdk.source.version>21</jdk.source.version>
    <org.eclipse.scout.rt.version>25.1.14</org.eclipse.scout.rt.version>
    <master_npm_release_dependency_mapping>--mapping.0.regex=@eclipse-scout --mapping.0.version=${org.eclipse.scout.rt.version}</master_npm_release_dependency_mapping>

    <!-- docker image build settings -->
    <!-- build target (push) -->
    <docker.app.image.registry>localhost:5000</docker.app.image.registry> <!-- set a desired registry before using profile exec.docker.image (e.g. registry.hub.docker.com/yourusername) -->
    <docker.app.image.tag>1.0.0-latest</docker.app.image.tag>
    <!-- java source image (pull) -->
    <docker.java.image.name>eclipse-temurin</docker.java.image.name>
    <docker.java.image.tag>21-jdk-jammy</docker.java.image.tag>
    <docker.java.image>${docker.java.image.name}:${docker.java.image.tag}</docker.java.image>
  </properties>

  <modules>
    <module>../inventory.shared</module>
    <module>../inventory.server</module>
    <module>../inventory.client</module>
    <module>../inventory.server.app</module>
    <module>../inventory.server.app.dev</module>
    <module>../inventory.server.app.image</module>
    <module>../inventory.server.app.zip</module>
    <module>../inventory.ui.html</module>
    <module>../inventory.ui.html.app</module>
    <module>../inventory.ui.html.app.dev</module>
    <module>../inventory.ui.html.app.image</module>
    <module>../inventory.ui.html.app.zip</module>
  </modules>

  <profiles>
    <profile>
      <!-- required for .app.image module to apply filtering on config.properties -->
      <id>filter.config.property</id>
      <activation>
        <file>
          <exists>${basedir}${file.separator}src${file.separator}main${file.separator}resources${file.separator}config.properties</exists>
        </file>
      </activation>
      <build>
        <resources>
          <resource>
            <directory>${project.basedir}${file.separator}src${file.separator}main${file.separator}resources</directory>
            <filtering>true</filtering>
          </resource>
        </resources>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>properties-maven-plugin</artifactId>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <!--  set property docker.app.image.registry before activating profile -->
      <id>exec.docker.image</id>
      <build>
        <pluginManagement>
          <plugins>
            <plugin>
              <!-- phase binding required for .app.image module to build and publish docker image -->
              <groupId>com.google.cloud.tools</groupId>
              <artifactId>jib-maven-plugin</artifactId>
              <executions>
                <execution>
                  <phase>package</phase>
                  <goals>
                    <goal>build</goal>
                  </goals>
                </execution>
              </executions>
            </plugin>
          </plugins>
        </pluginManagement>
      </build>
    </profile>
  </profiles>

  <dependencyManagement>
    <dependencies>
      <!-- Scout Dependencies -->
      <dependency>
        <groupId>org.eclipse.scout.rt</groupId>
        <artifactId>org.eclipse.scout.rt</artifactId>
        <version>${org.eclipse.scout.rt.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- Application Dependencies -->
      <dependency>
        <groupId>pers.mr.ft</groupId>
        <artifactId>inventory.client</artifactId>
        <version>${pers.mr.ft.inventory.version}</version>
      </dependency>
      <dependency>
        <groupId>pers.mr.ft</groupId>
        <artifactId>inventory.shared</artifactId>
        <version>${pers.mr.ft.inventory.version}</version>
      </dependency>
      <dependency>
        <groupId>pers.mr.ft</groupId>
        <artifactId>inventory.server</artifactId>
        <version>${pers.mr.ft.inventory.version}</version>
      </dependency>
      <dependency>
        <groupId>pers.mr.ft</groupId>
        <artifactId>inventory.server.app</artifactId>
        <version>${pers.mr.ft.inventory.version}</version>
      </dependency>
      <dependency>
        <groupId>pers.mr.ft</groupId>
        <artifactId>inventory.server.app.dev</artifactId>
        <version>${pers.mr.ft.inventory.version}</version>
      </dependency>
      <dependency>
        <groupId>pers.mr.ft</groupId>
        <artifactId>inventory.server.app.image</artifactId>
        <version>${pers.mr.ft.inventory.version}</version>
      </dependency>
      <dependency>
        <groupId>pers.mr.ft</groupId>
        <artifactId>inventory.server.app.zip</artifactId>
        <version>${pers.mr.ft.inventory.version}</version>
      </dependency>
      <dependency>
        <groupId>pers.mr.ft</groupId>
        <artifactId>inventory.ui.html</artifactId>
        <version>${pers.mr.ft.inventory.version}</version>
      </dependency>
      <dependency>
        <groupId>pers.mr.ft</groupId>
        <artifactId>inventory.ui.html.app</artifactId>
        <version>${pers.mr.ft.inventory.version}</version>
      </dependency>
      <dependency>
        <groupId>pers.mr.ft</groupId>
        <artifactId>inventory.ui.html.app.dev</artifactId>
        <version>${pers.mr.ft.inventory.version}</version>
      </dependency>
      <dependency>
        <groupId>pers.mr.ft</groupId>
        <artifactId>inventory.ui.html.app.image</artifactId>
        <version>${pers.mr.ft.inventory.version}</version>
      </dependency>
      <dependency>
        <groupId>pers.mr.ft</groupId>
        <artifactId>inventory.ui.html.app.zip</artifactId>
        <version>${pers.mr.ft.inventory.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>
</project>
